diff --git a/relax/backends/megatron/actor.py b/relax/backends/megatron/actor.py
index 4ac23c1..8206486 100644
--- a/relax/backends/megatron/actor.py
+++ b/relax/backends/megatron/actor.py
@@ -66,6 +66,10 @@ logging.getLogger("megatron").setLevel(logging.WARNING)
 logger = logging.getLogger(__name__)
 
 
+def _needs_multimodal_train_inputs(args: Namespace) -> bool:
+    return getattr(args, "multimodal_keys", None) is not None or getattr(args, "sglang_enable_multimodal", False)
+
+
 class MegatronTrainRayActor(TrainRayActor):
     def init(
         self,
@@ -422,7 +426,7 @@ class MegatronTrainRayActor(TrainRayActor):
                     "raw_reward",
                 ]
                 data_fields += ["rollout_routed_experts"] if self.args.use_rollout_routing_replay else []
-                if self.args.multimodal_keys is not None:
+                if _needs_multimodal_train_inputs(self.args):
                     data_fields.append("multimodal_train_inputs")
                 if self.args.use_opd and self.args.opd_type == "sglang":
                     data_fields.append("teacher_log_probs")
@@ -618,7 +622,7 @@ class MegatronTrainRayActor(TrainRayActor):
         batch_index = 0
         while not self.all_consumed("ref_log_probs", rollout_id):
             data_fields = ["tokens", "total_lengths", "response_lengths", "loss_masks", "rollout_log_probs"]
-            if self.args.multimodal_keys is not None:
+            if _needs_multimodal_train_inputs(self.args):
                 data_fields.append("multimodal_train_inputs")
             data, batch_meta = self._get_data_from_transfer_queue(
                 "ref_log_probs", rollout_id, data_fields, batch_size, batch_index
@@ -655,7 +659,7 @@ class MegatronTrainRayActor(TrainRayActor):
         batch_index = 0
         while not self.all_consumed("actor_log_probs", rollout_id):
             data_fields = ["tokens", "total_lengths", "response_lengths", "loss_masks", "rollout_log_probs"]
-            if self.args.multimodal_keys is not None:
+            if _needs_multimodal_train_inputs(self.args):
                 data_fields.append("multimodal_train_inputs")
             data, batch_meta = self._get_data_from_transfer_queue(
                 "actor_log_probs", rollout_id, data_fields, batch_size, batch_index
@@ -700,7 +704,7 @@ class MegatronTrainRayActor(TrainRayActor):
         ]
         if self.args.kl_coef != 0 or self.args.use_kl_loss:
             data_fields.append("ref_log_probs")
-        if self.args.multimodal_keys is not None:
+        if _needs_multimodal_train_inputs(self.args):
             data_fields.append("multimodal_train_inputs")
         if self.args.use_opd and self.args.opd_type == "sglang":
             data_fields.append("teacher_log_probs")
diff --git a/relax/utils/utils.py b/relax/utils/utils.py
index fb532de..2722319 100644
--- a/relax/utils/utils.py
+++ b/relax/utils/utils.py
@@ -75,7 +75,12 @@ def convert_samples_to_train_data(args: Any, samples: list[Sample] | list[list[S
     if samples[0].train_metadata is not None:
         train_data["metadata"] = [sample.train_metadata for sample in samples]
 
-    if samples[0].multimodal_train_inputs is not None:
+    if (
+        any(sample.multimodal_train_inputs is not None for sample in samples)
+        or samples[0].multimodal_inputs is not None
+        or getattr(args, "sglang_enable_multimodal", False)
+        or getattr(args, "multimodal_keys", None) is not None
+    ):
         train_data["multimodal_train_inputs"] = [sample.multimodal_train_inputs for sample in samples]
 
     if samples[0].teacher_log_probs is not None:
